spec:
  gateways:
  - istio-autogenerated-k8s-ingress.istio-system.svc.cluster.local
  hosts:
  - agile-devops.club
  http:
  - match:
    - headers:
        genero:
          exact: masc
      uri:
        prefix: /marte-xapi
    rewrite:
      uri: /xapi
    route:
    - destination:
        host: xapi
        port:
          number: 5000
        subset: man
  - match:
    - headers:
        genero:
          exact: girl
      uri:
        prefix: /marte-xapi
    rewrite:
      uri: /xapi
    route:
    - destination:
        host: xapi
        port:
          number: 5000
        subset: girl

